import React from 'react';

interface Props {}

export const Homepage = (props: Props) => {
    // const {} = props;

    return (
        <div>
            <h1> Homepage </h1>
        </div>
    );
}